import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.playfulrabbitdrift',
  appName: 'playful-rabbit-drift',
  webDir: 'dist'
};

export default config;
